# Use an official base image
FROM alpine:latest

# Install curl (just an example of installing a package)
RUN apk add --no-cache curl

# Simple command to execute when the container starts
CMD ["echo", "Hello from Docker!"]
